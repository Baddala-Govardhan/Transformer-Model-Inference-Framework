# Transformer-Model-Inference-Framework
A small inference framework I built to run different Hugging Face models (text, image, audio, image+text) through the same 4-step pipeline with an API layer for testing them over HTTP
